# Blog-Web-Application
This project is a simple Blog web application built using Node.js and Express. It allows users to create, view, edit, and delete blog posts.  Posts are stored temporarily in memory (no database), so they will reset when the server restarts.
